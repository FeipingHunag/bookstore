<!DOCTYPE html>
<html>
<head>
  <title>网上书店</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/kathleene/style", "web-app-theme/override", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/admin/books">网上书店后台</a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
          <li> <%= link_to "注销", logout_url, :method => :delete %></li>
        </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf">
         <li class="<%= controller.controller_path == 'admin/books' ? 'active' : '' %>"><a href="<%= admin_books_path %>">图书管理</a>
         </li>
         <li class="<%= controller.controller_path == 'admin/categories' ? 'active' : '' %>"><a href="<%= admin_categories_path %>">分类管理</a>
         </li>
         <li class="<%= controller.controller_path == 'admin/orders' ? 'active' : '' %>"><a href="<%= admin_orders_path %>">订单管理</a></li>
      </div>
    </div>
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> Web App.</p>
          </div>
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>
  </div>
</body>
</html>

